# Docker deployment environment variables
# Only infrastructure config goes here — API keys are configured via the Settings page

# No env vars needed by default. ANKI_CONNECT_URL is set in docker-compose.yml.
# Override here only if you need a custom AnkiConnect URL:
# ANKI_CONNECT_URL=http://anki:8765
